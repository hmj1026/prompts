# Anti-Rationalization Patterns

> **Canonical**: `${CLAUDE_PLUGIN_ROOT}/rules/anti-rationalization.md` (dhpk v0.4.0+) — 5 common self-persuasion phrasings + counter-arguments + load-trigger conditions.

## zdpos additions

- Historical rationalisation traps that bit this project specifically live in `memory/MEMORY.md` § "Judgment Feedback" (auto-memory directory) — load alongside the canonical rule when reviewing a borderline skip decision.
- Sentinel chain has 7 slots since dhpk v0.10.0（`.pending-migration-review` 已是官方 slot 6，zdpos 經 `mig:` extra paths opt-in）。"Sentinel auto-reaps" rationalisation still applies to the migration slot.
- Append-only exemption clause references `${CLAUDE_PLUGIN_ROOT}/rules/execution-policy.md` §0; zdpos-side overrides (if any) live in `.claude/rules/execution-policy.md`.
