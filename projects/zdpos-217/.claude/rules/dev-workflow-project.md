---
paths: []
---
# Dev Workflow — Project Override (zdpos_dev)

> 內容已移至 [`.claude/docs/dev-workflow-project.md`](../docs/dev-workflow-project.md)
> （2026-06-12 harness 健檢：本檔僅 `/dhpk:create-dev` / `dhpk:adaptive-dev-workflow`
> 情境需要，移出 rules/ auto-load 省每場 session 的 context）。
>
> dhpk skill 依約定讀本路徑（`@rules/dev-workflow-project.md`）— 請循上述指標載入
> docs 檔，取得 zdpos_dev 的 Repo Overrides、Profile Shortcut、Path→Test Hints 與 Examples。
